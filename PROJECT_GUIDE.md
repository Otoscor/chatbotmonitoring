# 🎓 프로젝트 완전 정복 가이드

> 이 문서는 프로그래밍을 처음 접하는 분들도 이해할 수 있도록 작성되었습니다.

---

## 📖 이 프로젝트는 뭐하는 거야?

**"캐릭터 챗봇 모니터링 시스템"**입니다!

쉽게 말하면, 여러 인터넷 커뮤니티(디시인사이드, 아카라이브 등)에서 **"캐릭터 챗봇"**에 관한 이야기들을 자동으로 모아서 분석해주는 프로그램이에요.

### 🤔 왜 필요해?

- 어떤 **캐릭터**가 요즘 인기있는지 알고 싶을 때
- 사람들이 어떤 **키워드**로 많이 이야기하는지 궁금할 때
- **인기 게시글**이 뭔지 한눈에 보고 싶을 때

이런 정보를 일일이 사이트를 돌아다니며 찾지 않아도, 이 프로그램이 자동으로 모아서 예쁘게 보여줍니다!

---

## 🏠 프로젝트 구조 설명

집을 지을 때 거실, 방, 주방이 있듯이, 이 프로젝트도 크게 **두 개의 방**으로 나뉘어 있어요.

```
📁 monitoring/
├── 📁 backend/    ← 🧠 두뇌 역할 (데이터 수집 & 분석)
├── 📁 frontend/   ← 👀 눈 역할 (화면에 보여주기)
└── 📄 README.md   ← 📖 설명서
```

---

## 🧠 백엔드 (Backend) - 두뇌 역할

> **"데이터를 수집하고, 저장하고, 분석하는 곳"**

### 📁 백엔드 폴더 구조

```
📁 backend/
├── 📁 api/          ← 🚪 "문" 역할 (외부와 소통하는 곳)
├── 📁 crawler/      ← 🕷️ "거미" 역할 (웹사이트에서 정보 긁어오기)
├── 📁 analyzer/     ← 📊 "분석가" 역할 (모은 정보 분석하기)
├── 📁 models/       ← 📦 "창고 설계도" (데이터 저장 방식 정의)
├── 📁 scheduler/    ← ⏰ "알람시계" 역할 (자동 실행 설정)
├── 📄 config.py     ← ⚙️ "설정" (프로그램 설정값들)
└── 📄 monitoring.db ← 💾 "창고" (실제 데이터 저장 파일)
```

### 1. 🕷️ 크롤러 (Crawler) - 정보 수집가

**크롤러**란? 웹사이트를 자동으로 돌아다니면서 정보를 긁어오는 프로그램이에요.

마치 **도서관에서 책을 찾아 복사해오는 사람**처럼, 인터넷 게시판에서 게시글 제목, 조회수, 추천수 같은 정보를 가져옵니다.

```
📁 crawler/
├── dcinside_crawler.py      ← 디시인사이드에서 정보 수집
├── arcalive_crawler.py      ← 아카라이브에서 정보 수집
├── zeta_crawler.py          ← 제타(캐릭터챗 서비스)에서 정보 수집
├── babechat_crawler.py      ← 베이브챗에서 정보 수집
├── lunatalk_crawler.py      ← 루나톡에서 정보 수집
├── crack_crawler.py         ← 크랙에서 정보 수집
└── multi_crawler.py         ← ⭐ 위의 크롤러들을 한번에 실행!
```

#### 수집하는 정보들:
| 정보 | 설명 |
|------|------|
| 제목 | 게시글 제목 |
| 작성자 | 누가 썼는지 |
| 조회수 | 몇 명이 봤는지 |
| 추천수 | 몇 명이 좋아요 눌렀는지 |
| 댓글수 | 댓글이 몇 개인지 |
| URL | 원본 게시글 링크 |

### 2. 📊 분석기 (Analyzer) - 데이터 분석가

수집한 정보를 **의미있게 분석**하는 곳이에요.

```
📁 analyzer/
├── keyword_extractor.py  ← 🔤 자주 나오는 단어 찾기
├── character_ranker.py   ← 👥 인기 캐릭터 순위 매기기
└── trend_analyzer.py     ← 📈 트렌드 분석하기
```

#### 분석하는 것들:
- **인기 키워드**: "뤼튼", "제타", "캐릭터" 같은 단어가 몇 번 나왔는지
- **캐릭터 순위**: 어떤 캐릭터가 가장 많이 언급되는지
- **트렌드**: 어제보다 오늘 더 많이 언급된 것은?

### 3. 📦 모델 (Models) - 데이터 설계도

데이터를 **어떤 형태로 저장할지** 정의하는 곳이에요.

마치 **서류함의 칸을 어떻게 나눌지** 정하는 것과 같아요.

```python
# 예시: 게시글 정보를 저장하는 설계도
게시글 = {
    "id": 1,                    # 고유 번호
    "제목": "제타 업데이트!",     # 게시글 제목
    "작성자": "익명",            # 누가 썼는지
    "조회수": 1234,             # 몇 명이 봤는지
    "추천수": 56,               # 좋아요 개수
    "댓글수": 23                # 댓글 개수
}
```

### 4. 🚪 API - 외부와 소통하는 문

**API(에이피아이)**란? 
프로그램끼리 대화하는 방법이에요.

마치 **레스토랑 웨이터**처럼:
1. 손님(프론트엔드)이 "인기 게시글 주세요!" 요청
2. 웨이터(API)가 주방(백엔드)에 전달
3. 주방에서 요리(데이터)를 준비
4. 웨이터가 손님에게 전달

```
손님(화면) ──요청──▶ 웨이터(API) ──▶ 주방(데이터베이스)
          ◀──응답──          ◀──
```

#### 사용하는 API 목록:

| API 주소 | 설명 | 예시 |
|----------|------|------|
| `/api/posts` | 게시글 목록 | 최근 게시글 50개 |
| `/api/posts/popular` | 인기 게시글 | 추천수 높은 게시글 |
| `/api/reports` | 일일 리포트 | 오늘의 통계 |
| `/api/reports/latest` | 최신 리포트 | 가장 최근 분석 결과 |
| `/api/keywords/trending` | 인기 키워드 | 자주 나온 단어들 |
| `/api/characters/ranking` | 캐릭터 순위 | 많이 언급된 캐릭터 |
| `/api/characters/chat-services` | 챗봇 서비스 캐릭터 | 제타, 베이브챗 등의 인기 캐릭터 |

---

## 👀 프론트엔드 (Frontend) - 눈 역할

> **"사용자에게 예쁘게 보여주는 곳"**

백엔드가 열심히 모은 데이터를, **사람이 보기 쉽게** 화면에 그려줍니다.

### 📁 프론트엔드 폴더 구조

```
📁 frontend/src/
├── 📁 pages/        ← 📄 각 페이지들 (대시보드, 리포트 등)
├── 📁 components/   ← 🧩 레고 블록 (재사용 가능한 조각들)
├── 📁 hooks/        ← 🎣 낚싯대 (데이터 가져오는 도구)
├── 📁 utils/        ← 🔧 도구함 (여러가지 편의 기능)
├── 📄 App.tsx       ← 🏠 메인 집 (모든 페이지 연결)
└── 📄 main.tsx      ← 🚀 시작점
```

### 1. 📄 페이지 (Pages)

웹사이트의 **각 화면**들이에요.

```
📁 pages/
├── Dashboard.tsx        ← 🏠 대시보드 (메인 화면)
├── Reports.tsx          ← 📊 리포트 목록
├── ReportDetail.tsx     ← 📋 리포트 상세 보기
├── CharacterRankings.tsx← 👥 캐릭터 순위
├── Characters.tsx       ← 🤖 캐릭터 목록
└── Keywords.tsx         ← 🔤 키워드 분석
```

#### 각 페이지 설명:

| 페이지 | URL | 설명 |
|--------|-----|------|
| 대시보드 | `/` | 전체 현황을 한눈에! 통계, 인기 게시글, 트렌드 차트 |
| 리포트 | `/reports` | 날짜별 분석 리포트 목록 |
| 리포트 상세 | `/reports/2025-01-19` | 특정 날짜의 자세한 분석 |
| 캐릭터 순위 | `/character-rankings` | 인기 캐릭터 TOP 순위 |

### 2. 🧩 컴포넌트 (Components)

**레고 블록**처럼 여러 페이지에서 재사용할 수 있는 조각들이에요.

```
📁 components/
├── Layout.tsx      ← 🖼️ 틀 (메뉴바, 전체 레이아웃)
├── StatCard.tsx    ← 📊 숫자 카드 (통계 표시)
├── KeywordCloud.tsx← ☁️ 키워드 구름 (단어 시각화)
└── RankingList.tsx ← 🏆 순위 목록 (TOP 10 표시)
```

### 3. 🎣 훅 (Hooks)

데이터를 **쉽게 가져오는 도구**예요.

```typescript
// useApi.ts - API에서 데이터 가져오는 낚싯대
const { data, loading, error } = useApi(fetchLatestReport)

// data: 가져온 데이터
// loading: 아직 가져오는 중인지 (true/false)
// error: 에러가 났는지
```

### 4. 🔧 유틸리티 (Utils)

**편의 기능**들을 모아둔 곳이에요.

```
📁 utils/
└── api.ts  ← 📞 API 호출 함수들 모음
```

---

## 🔄 데이터 흐름 (어떻게 동작하나요?)

전체 동작 과정을 그림으로 보면:

```
┌─────────────────────────────────────────────────────────────┐
│  1️⃣ 크롤러가 웹사이트에서 데이터 수집                          │
│                                                             │
│    디시인사이드 ─┐                                           │
│    아카라이브  ──┼──▶ 🕷️ 크롤러 ──▶ 💾 데이터베이스           │
│    제타/베이브챗 ─┘                                           │
└─────────────────────────────────────────────────────────────┘
                              ⬇️
┌─────────────────────────────────────────────────────────────┐
│  2️⃣ 분석기가 데이터 분석                                      │
│                                                             │
│    💾 데이터베이스 ──▶ 📊 분석기 ──▶ 인기 키워드              │
│                                    인기 캐릭터               │
│                                    트렌드                    │
└─────────────────────────────────────────────────────────────┘
                              ⬇️
┌─────────────────────────────────────────────────────────────┐
│  3️⃣ API가 데이터 전달                                        │
│                                                             │
│    💾 데이터베이스 ──▶ 🚪 API ──▶ 📱 프론트엔드              │
└─────────────────────────────────────────────────────────────┘
                              ⬇️
┌─────────────────────────────────────────────────────────────┐
│  4️⃣ 프론트엔드가 예쁘게 화면에 표시                           │
│                                                             │
│    📊 차트      📋 표      🏆 순위      ☁️ 키워드 구름        │
│         ↓         ↓          ↓             ↓                │
│    ┌─────────────────────────────────────────────┐          │
│    │               👀 사용자 화면                 │          │
│    └─────────────────────────────────────────────┘          │
└─────────────────────────────────────────────────────────────┘
```

---

## 🛠️ 사용된 기술들 (초보자 버전)

### 백엔드 기술

| 기술 | 역할 | 비유 |
|------|------|------|
| **Python** | 프로그래밍 언어 | 🗣️ 컴퓨터와 대화하는 언어 |
| **FastAPI** | 웹 서버 | 🏪 가게 (요청을 받고 응답) |
| **SQLite** | 데이터베이스 | 📁 파일 서랍장 (데이터 저장) |
| **SQLAlchemy** | DB 연결 도구 | 🔌 서랍장과 연결하는 케이블 |
| **BeautifulSoup** | HTML 파싱 | 🔍 웹페이지에서 원하는 부분 찾기 |
| **Playwright** | 브라우저 자동화 | 🤖 자동으로 웹서핑하는 로봇 |

### 프론트엔드 기술

| 기술 | 역할 | 비유 |
|------|------|------|
| **React** | UI 라이브러리 | 🧩 화면 만드는 레고 블록 |
| **TypeScript** | 프로그래밍 언어 | 🗣️ 더 안전한 JavaScript |
| **Vite** | 빌드 도구 | ⚡ 빠른 요리사 (코드를 빠르게 준비) |
| **TailwindCSS** | 스타일링 | 🎨 옷 입히기 (예쁘게 꾸미기) |
| **Recharts** | 차트 라이브러리 | 📊 그래프 그리기 도구 |
| **Axios** | HTTP 요청 도구 | 📮 우편배달부 (API와 소통) |

---

## 📡 API 사용 방법 (상세)

### 게시글 관련 API

#### 1. 게시글 목록 가져오기
```
GET /api/posts?skip=0&limit=50
```
- `skip`: 몇 개 건너뛸지 (0부터 시작)
- `limit`: 몇 개 가져올지 (최대 100개)

#### 2. 인기 게시글 가져오기
```
GET /api/posts/popular?limit=15&days=7
```
- `limit`: 몇 개 가져올지
- `days`: 최근 며칠 기준인지

### 리포트 관련 API

#### 1. 리포트 목록
```
GET /api/reports?skip=0&limit=30
```

#### 2. 최신 리포트
```
GET /api/reports/latest
```

#### 3. 특정 날짜 리포트
```
GET /api/reports/2025-01-19
```

### 키워드/캐릭터 API

#### 1. 인기 키워드
```
GET /api/keywords/trending?days=7&limit=20
```

#### 2. 캐릭터 순위
```
GET /api/characters/ranking?days=7&limit=20
```

#### 3. 챗봇 서비스 캐릭터
```
GET /api/characters/chat-services?service=zeta&limit=30
```
- `service`: zeta, babechat, lunatalk, crack 중 선택

---

## 🗄️ 데이터베이스 구조

### 테이블 설명 (표로 정리)

#### 1. posts (게시글 테이블)
| 컬럼명 | 타입 | 설명 |
|--------|------|------|
| id | 숫자 | 고유 번호 (자동 증가) |
| post_id | 문자 | 원본 게시글 ID |
| gallery_id | 문자 | 게시판 ID (wrtnai, aichatting 등) |
| title | 문자 | 게시글 제목 |
| author | 문자 | 작성자 |
| view_count | 숫자 | 조회수 |
| recommend_count | 숫자 | 추천수 |
| comment_count | 숫자 | 댓글수 |
| url | 문자 | 원본 링크 |
| crawled_at | 날짜 | 수집된 시간 |

#### 2. daily_reports (일일 리포트 테이블)
| 컬럼명 | 타입 | 설명 |
|--------|------|------|
| id | 숫자 | 고유 번호 |
| report_date | 날짜 | 리포트 날짜 |
| total_posts | 숫자 | 총 게시글 수 |
| total_views | 숫자 | 총 조회수 |
| top_keywords | JSON | 인기 키워드 목록 |
| top_characters | JSON | 인기 캐릭터 목록 |

#### 3. chat_service_characters (챗봇 서비스 캐릭터)
| 컬럼명 | 타입 | 설명 |
|--------|------|------|
| id | 숫자 | 고유 번호 |
| service | 문자 | 서비스명 (zeta, babechat 등) |
| rank | 숫자 | 순위 |
| name | 문자 | 캐릭터 이름 |
| author | 문자 | 제작자 |
| views | 숫자 | 조회수 |
| tags | JSON | 태그 목록 |

---

## 🚀 실행 방법

### 1. 백엔드 실행 (데이터 수집 및 API 서버)

```bash
# 1. backend 폴더로 이동
cd backend

# 2. 가상환경 만들기 (처음 한번만)
python -m venv venv

# 3. 가상환경 활성화
source venv/bin/activate   # Mac/Linux
# 또는
venv\Scripts\activate      # Windows

# 4. 필요한 패키지 설치 (처음 한번만)
pip install -r requirements.txt

# 5. API 서버 실행
python -m api.main
```

### 2. 프론트엔드 실행 (화면 보기)

```bash
# 1. frontend 폴더로 이동
cd frontend

# 2. 필요한 패키지 설치 (처음 한번만)
npm install

# 3. 개발 서버 실행
npm run dev
```

이후 브라우저에서 `http://localhost:3000` 접속!

---

## 🌐 배포 방식

이 프로젝트는 **GitHub Pages**로 무료 배포됩니다.

```
📝 데이터 업데이트 과정:

1. 로컬에서 크롤링 실행 (데이터 수집)
2. 수집된 데이터를 JSON 파일로 변환
3. GitHub에 업로드 (push)
4. GitHub Actions가 자동으로 빌드
5. GitHub Pages에서 웹사이트 업데이트!
```

한 줄로 실행:
```bash
./update_site.sh
```

---

## 📚 용어 사전

| 용어 | 설명 |
|------|------|
| **크롤링 (Crawling)** | 웹사이트에서 자동으로 정보를 수집하는 것 |
| **API** | 프로그램끼리 대화하는 규칙/방법 |
| **데이터베이스** | 데이터를 저장하는 창고 |
| **프론트엔드** | 사용자가 보는 화면 |
| **백엔드** | 서버에서 데이터를 처리하는 부분 |
| **컴포넌트** | 재사용 가능한 UI 조각 |
| **JSON** | 데이터를 표현하는 형식 `{"key": "value"}` |
| **REST API** | 웹에서 데이터를 주고받는 표준 방식 |

---

## 🎯 핵심 정리

```
이 프로젝트 = 자동 뉴스 수집기 + 분석가 + 신문사

1. 🕷️ 크롤러: 여러 사이트를 돌아다니며 정보 수집
2. 📊 분석기: 수집한 정보에서 트렌드 발견
3. 🚪 API: 분석 결과를 전달하는 창구
4. 👀 프론트엔드: 예쁘게 화면에 표시
```

---

## ❓ 자주 묻는 질문

### Q: 데이터는 어디에 저장되나요?
A: `backend/monitoring.db` 파일에 SQLite 형태로 저장됩니다.

### Q: 크롤링은 얼마나 자주 하나요?
A: 수동으로 실행하거나, 스케줄러를 설정하면 자동 실행됩니다.

### Q: 새로운 사이트를 추가하려면?
A: `backend/crawler/` 폴더에 새 크롤러 파일을 만들고, `config.py`에 등록합니다.

### Q: 프론트엔드에 새 페이지를 추가하려면?
A: `frontend/src/pages/`에 새 파일을 만들고, `App.tsx`에 라우트를 추가합니다.

---

## 📞 도움이 필요하다면?

이 가이드를 읽고도 이해가 안 되는 부분이 있다면, 언제든 질문해주세요! 
프로그래밍은 처음에 어렵지만, 차근차근 배우면 누구나 할 수 있습니다. 💪

---

*이 문서는 초보자를 위해 작성되었습니다. 더 자세한 기술 문서는 `README.md`를 참고하세요.*
